# Scripts

Local development helpers:

```bash
./scripts/dev-up.sh
./scripts/dev-down.sh
```

`dev-up.sh` starts the Docker Compose development stack. `dev-down.sh` stops it and removes named volumes for a clean local reset.
